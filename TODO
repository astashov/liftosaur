* Programs
  - 5/3/1s
    - BBB
    - Building the monolith
  - Greyskull LP
  - GZCL Method
  - GZCLP
  - Strong Curves
  - nSuns LP
* Add errors handling to HTTP calls
* Extract lens lib into a separate npm package
* Add exporting all data to downloadable CSV
* Add deleting account
* Add achievements
* Add cookie banner
* Add ability to publish user programs
* Add changing program day when editing progress day
* Versionize published programs
* Lazy load Codemirror
* Fix backups
* Add comments support to parser
* Think of a better way to pass pieces of state into lens play
* Refactor MenuItem, to better incorporate existing use cases
* Split editProgramExcercise into multiple files

* Bug:
This is valid code:
```
if (completedReps[1][1] >= reps[1][1]) {
  state.benchPressWeight = weights + 5lb
}
```

* Bug:
If you use unexisting state var in code, and then add it, it won't recognize it.

* Bug:
Assign lb to state var of type number